configuration:
    images_directory:
        name: Image directory
        description: Folder to take images from
    period_seconds:
        name: Period
        description: Number of seconds each image will show for
    templates_folder:
        name: Templates folder
        description: Path to custom flask templates folder to use instead of default
